[AWS]
; aws access key
ACCESS_KEY =
; aws secret access key
SECRET_ACCESS_KEY =
; s3 bucket where the Python script can be copied to
S3_BUCKET =
; folder within S3_BUCKET where the Python script is to be copied to, empty if root folder
S3_FOLDER =
; region e.g. ap-southeast-2
REGION =
; ec2 instance key pair name (for this region)
EC2_KEYPAIR =
; s3 directory of the data files
S3_DATA_URI =
